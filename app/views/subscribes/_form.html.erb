<%= form_with(model: subscribe, local: true) do |form| %>
  <% if subscribe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subscribe.errors.count, "error") %> prohibited this subscribe from being saved:</h2>

      <ul>
      <% subscribe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :is_mail %>
    <%= form.check_box :is_mail, id: :subscribe_is_mail %>
  </div>

  <div class="field">
    <%= form.label :is_message %>
    <%= form.check_box :is_message, id: :subscribe_is_message %>
  </div>

  <div class="field">
    <%= form.label :topic_id %>
    <%= form.text_field :topic_id, id: :subscribe_topic_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :subscribe_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
